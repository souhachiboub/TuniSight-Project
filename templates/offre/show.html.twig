{% extends 'offre/templateadmin.html.twig' %} 
{% block title %}Liste des Offres{% endblock %}
{% block body %}

    <style>
        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }
        .pagination ul {
            list-style: none;
            padding: 0;
        }
        .pagination li {
            display: inline-block;
            margin: 0 5px;
        }
        .pagination a {
            color: #7AB2D3;
            text-decoration: none;
            padding: 5px 10px;
            border: 1px solid #7AB2D3;
            border-radius: 3px;
        }
        .pagination a:hover {
            background-color: #7AB2D3;
            color: white;
        }
        .pagination .active a {
            background-color: #7AB2D3;
            color: white;
            border-color: #7AB2D3;
        }
        .btn-custom {
            background-color: #7AB2D3;
            color: white;
            border: none;
        }
        .btn-custom:hover {
            background-color: #5C97BF;
        }
    </style>

    <div class="container-fluid">
        <h1 class="h3 mb-2 text-gray-800">Liste des Offres</h1>

        <div id="flashMessageContainer">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            {% endfor %}
        </div>

        <div class="mb-4">
            <button type="button" class="btn btn-custom" data-toggle="modal" data-target="#addOffreModal">
                <i class="fas fa-plus"></i> Ajouter une nouvelle offre
            </button>
        </div>

        <div class="card shadow p-3 mb-4">
            <form id="filter-form" class="row g-3 align-items-center">
                <div class="col-md-4">
                    <label for="expired" class="form-label">État d'expiration</label>
                    <select name="expired" id="expired" class="form-control w-auto">
                        <option value="">Tous</option>
                        <option value="1">Expirés</option>
                        <option value="0">Non expirés</option>
                    </select>
                </div>
                <div class="col-md-4 d-flex align-items-end">
                    <button type="submit" class="btn btn-custom w-100">
                        <i class="fas fa-filter"></i> Filtrer
                    </button>
                </div>
            </form>
        </div>

        <div id="offre_list" class="row">
            {% include 'offre/_offres.html.twig' %}
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#filter-form').on('submit', function(event) {
                event.preventDefault();
                let expired = $('#expired').val();
                
                $.ajax({
                    url: "{{ path('offre_show') }}",
                    method: "GET",
                    data: { expired: expired },
                    success: function(response) {
                        $('#offre_list').html($(response).find('#offre_list').html());
                        $('.pagination').html($(response).find('.pagination').html());
                    }
                });
            });
        });
    </script>
{% endblock %}
