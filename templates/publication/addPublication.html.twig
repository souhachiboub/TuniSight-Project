{% block body %}

<button class="btn-primary" data-modal="publicationModal">
    <img src="{{ asset('client/images/write.png') }}" alt="Ajouter Publication" style="width: 30px; height: 30px;"  title="Ajouter une publication">
</button>

    <div id="publicationModal" class="modal">
        <div class="modal-content">
            <a href="#" class="close">&times;</a>
            <div class="logo" style="text-align: center;">
                <h2>Publier une nouvelle publication</h2>
                {{ form_start(form, {'attr': {'id': 'publicationForm', 'novalidate': 'novalidate'}}) }}

                <div class="input-group">
                    <label for="{{ form.contenu.vars.id }}"></label>
                    {{ form_widget(form.contenu, {
                        'attr': {
                            'rows': '1',
                            'cols': '33',
                            'placeholder': 'Écrivez votre publication',
                            'required': 'required',
                            'class': 'textarea'
                        }
                    }) }}
                </div>
                    {% for error in form.contenu.vars.errors %}
                        <p class="erroe" style="color: red;" class="text-danger mt-1">{{ error.message }}</p>
                    {% endfor %}

                    <label for="{{ form.images.vars.id }}"></label>
                    {{ form_widget(form.images, {
                        'attr': {
                            'class': 'file-input',
                            'style': 'display: none;'  
                        }
                    }) }}
                    
                    <button type="button" class="file-upload-btn" onclick="document.getElementById('{{ form.images.vars.id }}').click();">
                        <img src="{{ asset('client/images/upload.png') }}" alt="Upload" style="width: 40px; height: 40px;">
                    </button>
                                    
                    <div id="imagePreview" style="display: flex; flex-wrap: wrap; margin-top: 10px;"></div>
                

                <button type="submit" class="submit-btn">Publier</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
    
    {%block js%}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const modals = document.querySelectorAll('.modal');
            const openModalButtons = document.querySelectorAll('[data-modal]');
            const closeModalButtons = document.querySelectorAll('.close');
        
            function clearForm(modal) {
                const form = modal.querySelector('form');
                if (form) {
                    // Réinitialiser les champs texte et textarea, mais pas le fichier
                    form.querySelectorAll('input:not([type="file"]), textarea').forEach(input => {
                        input.value = '';
                    });
        
                    // Vider uniquement l'aperçu des images
                    const previewContainer = modal.querySelector('#imagePreview');
                    if (previewContainer) {
                        previewContainer.innerHTML = '';
                    }
        
                    // Réinitialiser l'input file sans affecter l'aperçu des images
                    const imageInput = modal.querySelector('input[type="file"]');
                    if (imageInput) {
                        const newInput = imageInput.cloneNode();
                        imageInput.replaceWith(newInput);
                        setupImagePreview(newInput, previewContainer);
                    }
                    
                }
            }
            const form = document.getElementById('publicationForm');
        
            function setupImagePreview(imageInput, previewContainer) {
                imageInput.addEventListener('change', function(event) {
                    let files = Array.from(event.target.files);
                    let existingFiles = previewContainer.querySelectorAll('img').length;
        
                    files.forEach((file, index) => {
                        let reader = new FileReader();
        
                        reader.onload = function(e) {
                            let imgContainer = document.createElement('div');
                            imgContainer.style.position = "relative";
                            imgContainer.style.display = "inline-block";
                            imgContainer.style.margin = "5px";
        
                            let img = document.createElement('img');
                            img.src = e.target.result;
                            img.style.maxWidth = "120px";
                            img.style.height = "120px";
                            img.style.borderRadius = "5px";
                            img.style.objectFit = "cover";
        
                            // Bouton de suppression
                            let removeBtn = document.createElement('button');
                            removeBtn.innerHTML = "&times;";
                            removeBtn.style.position = "absolute";
                            removeBtn.style.top = "5px";
                            removeBtn.style.right = "5px";
                            removeBtn.style.background = "rgba(0, 0, 0, 0.7)";
                            removeBtn.style.color = "white";
                            removeBtn.style.border = "none";
                            removeBtn.style.cursor = "pointer";
                            removeBtn.style.borderRadius = "50%";
                            removeBtn.style.width = "20px";
                            removeBtn.style.height = "20px";
                            removeBtn.style.fontSize = "16px";
                            removeBtn.style.display = "flex";
                            removeBtn.style.alignItems = "center";
                            removeBtn.style.justifyContent = "center";
        
                            removeBtn.addEventListener('click', function() {
                                imgContainer.remove();
                            });
        
                            imgContainer.appendChild(img);
                            imgContainer.appendChild(removeBtn);
                            previewContainer.appendChild(imgContainer);
                        };
        
                        reader.readAsDataURL(file);
                    });
                });
            }
        
            openModalButtons.forEach(button => {
                button.addEventListener('click', function(event) {
                    event.preventDefault();
                    const modalId = button.getAttribute('data-modal');
        
                    modals.forEach(modal => {
                        modal.style.display = 'none';
                        clearForm(modal);
                    });
        
                    const modal = document.getElementById(modalId);
                    if (modal) {
                        modal.style.display = 'block';
                    }
                });
            });
        
            closeModalButtons.forEach(button => {
                button.addEventListener('click', function(event) {
                    event.preventDefault();
        
                    const modal = button.closest('.modal');
                    if (modal) {
                        modal.style.display = 'none';
                        clearForm(modal);
                        
                    }
                });
            });
        
            // Fermer le modal en cliquant en dehors
            window.addEventListener('click', function(event) {
                modals.forEach(modal => {
                    if (event.target === modal) {
                        modal.style.display = 'none';
                        clearForm(modal);
                    }
                });
            });
        
            // Initialisation de la prévisualisation pour chaque input file
            document.querySelectorAll('input[type="file"]').forEach(input => {
                const previewContainer = document.getElementById('imagePreview');
                setupImagePreview(input, previewContainer);
            });
        });
        

        document.addEventListener("DOMContentLoaded", function () {
            const textarea = document.querySelector("textarea");

            textarea.addEventListener("input", function () {
                this.style.height = "auto";
                this.style.height = this.scrollHeight + "px";
            });
        });
        document.addEventListener("DOMContentLoaded", function() {
            document.querySelectorAll("textarea").forEach(textarea => {
                textarea.style.overflow = "hidden"; // Hide scrollbar
                textarea.style.height = textarea.scrollHeight + "px"; // Ajuste la hauteur initiale
                textarea.addEventListener("input", function() {
                    this.style.height = "auto"; // Réinitialise la hauteur
                    this.style.height = (this.scrollHeight) + "px"; // Ajuste selon le contenu
                });
            });
        });
    </script>
    {% endblock %}
    {%block css%}
    <style>
        
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 2000;
            overflow-y: auto;
            font-family: system-ui;
        }

        .modal-content {
            background-color: #ffffff;
            margin: 10% auto;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
            width: 90%;
            max-width: 600px;
            text-align: left;
            position: relative;
        }

        h2 {
            font-weight: normal;
            color: #26648E;
            text-align: center;
        }

        .input-group {
            margin-bottom: 20px;
        }

        .input-group label {
            color: #26648E;
            margin-bottom: 8px;
            display: inline-block;
        }
        
    
        .input-group textarea {
            width: 100%;
            min-height : 45px;
            padding: 10px;
            height : auto ; 
            border: 1px solid #ccc;
            border-radius: 5px;
            font-family: sans-serif; /* Police de caractères */
            font-size: 16px; /* Taille de la police */
            line-height: 1.5; /* Hauteur de ligne */
            box-sizing: border-box; /* Pour inclure le padding et la bordure dans la largeur */
            transition: border-color 0.3s ease; /* Animation sur la bordure */
            resize: none;
            overflow-y: auto;
    
        }
    
        .input-group textarea:focus {
            border-color: #007bff; /* Couleur de la bordure au focus */
            outline: none; /* Supprimer l'outline par défaut */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.2); /* Ombre légère au focus */
            
        }
    
        .input-group textarea::placeholder {
            color: #aaa; /* Couleur du placeholder */
        }
        .file-input {
            font-family: system-ui;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
        }

        .submit-btn {
            width: 100%;
            padding: 12px 20px;
            background-color: #26648E;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 16px;
            font-weight: 500;
            font-family: system-ui;
        }

        .submit-btn:hover {
            background-color: #4F8FC0;
            transform: translateY(-1px);
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .modal a.close {
            text-decoration: none;
            font-size: 30px;
            position: absolute;
            top: 5px;
            right: 10px;
            color: #26648E;
            cursor: pointer;
            font-weight: normal;
        }
        .file-upload-btn {
            padding: 8px;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            transition: all 0.3s ease;
            background-color: #f4f4f4;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px; /* Ajuste la taille du bouton */
            height: 50px;
            margin-top: 5px;
            margin-right: 50px;
        }
        
        .file-upload-btn img {
            border-radius: 50%;
            width: 50px; 
            height: 24px;
        }
        
        .file-upload-btn:hover {
            opacity: 0.8;
            transform: scale(1.05);
        }
        
        
        .file-input {
            font-family: system-ui;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            display: none; 
        }
        
        .file-upload-btn {
            background-color: white;
            border-radius: 4px;
            font-size: 30px;
        }
        

        .btn-primary {
            background-color:white;
            color: white;
            border: none;
            padding: 20px 20px;
            font-size: 16px;
            border-radius: 50%;
            cursor: pointer;
            transition: all 0.3s ease;
            bottom: -120px; /* Distance du bas de l'écran */
            right: 300px; /* Distance du côté gauche de l'écran */
            position : absolute ;
        }
        
        .btn-primary:hover {
            background-color: #4F8FC0;
        }
        

        @media (max-width: 768px) {
            .modal-content {
                margin: 15% auto;
                padding: 20px;
                width: 80%;
            }

            h2 {
                font-size: 1.4rem;
            }

            .submit-btn {
                font-size: 14px;
                padding: 10px 15px;
            }

            .input-group textarea {
                font-size: 0.9rem;
                padding: 8px;
            }
        }

        @media (max-width: 480px) {
            .modal-content {
                margin: 20% auto;
                padding: 15px;
                width: 90%;
            }

            h2 {
                font-size: 1.2rem;
            }

            .submit-btn {
                font-size: 14px;
                padding: 8px 12px;
            }

            .input-group textarea {
                font-size: 0.8rem;
                padding: 6px;
            }
        }
    </style>
{% endblock %}
